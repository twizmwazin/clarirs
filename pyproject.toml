[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "clarirs"
requires-python = ">=3.10"
dynamic = ["version"]

[tool.maturin]
manifest-path = "crates/clarirs_py/Cargo.toml"
include = ["README.md", "LICENSE"]

[dependency-groups]
dev = [
    "pytest>=8.3.5",
]
